/** localStorage keys — keep in sync with any migration logic. */
export const STORAGE_KEYS = {
  users: "juliamakeup-users",
  session: "juliamakeup-session",
  reservations: "juliamakeup-reservations",
};

export const TOAST_MS = 5200;
